# Document Lookup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a dedicated `查单号` WPS add-in flow that lets the user choose one OA or ERP document from a filtered dropdown and writes a fixed `单号查询结果` worksheet with material-level OA/ERP side-by-side comparison.

**Architecture:** Keep this as a separate single-document lookup path instead of folding it into the existing current-sheet query path. Reuse existing source table readers, workbook helpers, batch matrix writer, and metadata cleanup, while adding a focused core builder, a dedicated static dialog, and a dedicated macro bridge.

**Tech Stack:** TypeScript strict mode, Vitest, decimal.js-light, esbuild, WPS JS runtime, static ES5-style dialog JavaScript, npm.

---

## File Structure

- Modify: `src/constants.ts`
  - Add `SHEET_NAMES.documentLookup`.
  - Add `DOCUMENT_LOOKUP_HEADERS` for the fixed result sheet contract.

- Modify: `src/types/scrap.ts`
  - Add `DocumentLookupOutputKind = "document_lookup"`.
  - Extend `OutputMetadataKind` to include `DocumentLookupOutputKind` without adding document lookup to `OutputSheetKind`.

- Modify: `src/wps-api/output-metadata.ts`
  - Accept `document_lookup` as valid metadata.
  - Allow `clearPreviousToolOutput()` to clear any `OutputMetadataKind`, while keeping compatible legacy kinds restricted to `LegacyOutputSheetKind[]`.

- Modify: `tests/wps-api/output-metadata.test.ts`
  - Lock document lookup metadata read/write/clear behavior.

- Modify: `tests/core/output-sheets.test.ts`
  - Assert `单号查询结果` is not treated as a current-sheet query/expand output sheet.

- Create: `src/core/document-lookup.ts`
  - Build OA/ERP dropdown suggestions.
  - Build material-level side-by-side lookup rows for a selected OA or ERP document.
  - Convert lookup rows to a worksheet matrix.

- Create: `tests/core/document-lookup.test.ts`
  - Cover suggestions, OA lookup, ERP lookup, missing counterpart documents, one-sided materials, duplicated material rows, multiple counterpart document numbers, no-result messages, and output headers.

- Create: `ui/document-lookup-dialog.html`
  - Static WPS dialog for lookup mode, filtered candidate input, and action buttons.

- Create: `ui/document-lookup-dialog.js`
  - Conservative browser-compatible dialog script that reads token-scoped suggestions, filters candidates by containment, forces selection from the dropdown, and writes structured results.

- Create: `tests/query-dialog/document-lookup-static.test.ts`
  - VM-based tests for the static dialog behavior.

- Create: `src/query-dialog/open-document-lookup-dialog.ts`
  - Token, `PluginStorage`, `ShowDialog`, polling, cleanup, and result parsing for the document lookup dialog.

- Create: `tests/query-dialog/open-document-lookup-dialog.test.ts`
  - Bridge tests for URL, initial suggestions, submit/cancel/stale token, cleanup, and timeout behavior.

- Create: `src/macros/document-lookup.ts`
  - Read source rows, open lookup dialog, run selected lookup, create/clear/write `单号查询结果`, and save metadata.

- Create: `tests/macros/document-lookup.test.ts`
  - Macro integration tests around source reads, sheet creation/reuse, metadata cleanup, no-result writes, and source errors.

- Modify: `src/actions/button-actions.ts`
  - Register `btnLookupDocument`.

- Modify: `src/main.ts`
  - Bind `lookupDocument()` to the new macro.

- Modify: `ribbon.xml`
  - Add a `查单号` ribbon button.

- Modify: `tests/actions/button-actions.test.ts`
  - Assert the new button is registered.

- Modify: `tests/build/build-output.test.ts`
  - Assert new static dialog files ship, `ribbon.xml` contains the new button, and generated `main.js` contains the new action.

- Modify: `main.js`
  - Regenerated by `npm run build`; do not edit by hand.

---

### Task 1: Add Document Lookup Output Contract And Metadata Kind

**Files:**
- Modify: `src/constants.ts`
- Modify: `src/types/scrap.ts`
- Modify: `src/wps-api/output-metadata.ts`
- Test: `tests/wps-api/output-metadata.test.ts`
- Test: `tests/core/output-sheets.test.ts`

- [ ] **Step 1: Write the failing metadata and output-sheet tests**

Add this import in `tests/core/output-sheets.test.ts` if it is not already present:

```ts
import { SHEET_NAMES } from "../../src/constants";
```

Extend the `"does not treat source or diagnostics sheets as output sheets"` test with:

```ts
    expect(detectOutputSheetKind(SHEET_NAMES.documentLookup)).toBeNull();
```

Add these tests inside `describe("output metadata", () => { ... })` in `tests/wps-api/output-metadata.test.ts` after the variance summary metadata test:

```ts
  it("stores and reads document lookup metadata", () => {
    const sheet = createFakeSheet("单号查询结果");

    saveOutputMetadata(sheet, { kind: "document_lookup", rangeAddress: "A1:Z4" });

    expect(readOutputMetadata(sheet)).toEqual({ kind: "document_lookup", rangeAddress: "A1:Z4" });
  });

  it("clears document lookup output by metadata range", () => {
    const sheet = createFakeSheet("单号查询结果");
    saveOutputMetadata(sheet, { kind: "document_lookup", rangeAddress: "A1:Z4" });

    clearPreviousToolOutput(sheet, "document_lookup");

    expect(sheet.clears).toEqual(["A1:Z4"]);
  });
```

- [ ] **Step 2: Run focused tests and verify they fail**

Run:

```bash
npm test -- tests/wps-api/output-metadata.test.ts tests/core/output-sheets.test.ts
```

Expected: FAIL because `SHEET_NAMES.documentLookup` and metadata kind `document_lookup` do not exist.

- [ ] **Step 3: Add the result sheet name and header contract**

Modify `src/constants.ts`.

Add this property to `SHEET_NAMES` after `erpDocCompare`:

```ts
  documentLookup: "单号查询结果",
```

Add this header constant after `ERP_DOC_COMPARE_HEADERS`:

```ts
export const DOCUMENT_LOOKUP_HEADERS = [
  "行类型",
  "查询类型",
  "命中单号",
  "OA表单编号",
  "OA记录的ERP单号",
  "OA申请日期",
  "OA公司简称",
  "OA一级部门",
  "OA二级部门",
  "OA物料编码",
  "OA物料名称",
  "OA数量",
  "OA金额",
  "ERP单据编号",
  "ERP记录的OA单号",
  "ERP日期",
  "ERP公司简称",
  "ERP一级部门",
  "ERP二级部门",
  "ERP物料编码",
  "ERP物料名称",
  "ERP数量",
  "ERP金额",
  "数量差额",
  "金额差额",
  "备注"
] as const;
```

- [ ] **Step 4: Add a metadata-only output kind**

Modify `src/types/scrap.ts` around the output kind types:

```ts
export type DocCompareRowType = "汇总" | "物料";
export type OutputSheetKind = "variance_summary" | "oa_doc_compare" | "erp_doc_compare";
export type LegacyOutputSheetKind = "legacy_detail";
export type DocumentLookupOutputKind = "document_lookup";
export type OutputMetadataKind = OutputSheetKind | LegacyOutputSheetKind | DocumentLookupOutputKind;
```

- [ ] **Step 5: Accept document lookup metadata and widen cleanup expected kind**

Modify imports and constants in `src/wps-api/output-metadata.ts`:

```ts
import type { LegacyOutputSheetKind, OutputMetadataKind, RibbonQueryState } from "../types/scrap";
```

Update `VALID_OUTPUT_KINDS`:

```ts
const VALID_OUTPUT_KINDS = new Set<OutputMetadataKind>([
  "variance_summary",
  "legacy_detail",
  "oa_doc_compare",
  "erp_doc_compare",
  "document_lookup"
]);
```

Update the `clearPreviousToolOutput()` signature:

```ts
export function clearPreviousToolOutput(
  sheet: WpsSheet,
  expectedKind: OutputMetadataKind,
  compatibleLegacyKinds: LegacyOutputSheetKind[] = []
): void {
  const metadata = readOutputMetadata(sheet);
  if (!metadata || (metadata.kind !== expectedKind && !isCompatibleLegacyKind(metadata.kind, compatibleLegacyKinds))) {
    return;
  }

  clearRange(sheet, metadata.rangeAddress);
}
```

- [ ] **Step 6: Run focused tests and verify they pass**

Run:

```bash
npm test -- tests/wps-api/output-metadata.test.ts tests/core/output-sheets.test.ts
```

Expected: PASS.

- [ ] **Step 7: Commit the output contract**

Run:

```bash
git add src/constants.ts src/types/scrap.ts src/wps-api/output-metadata.ts tests/wps-api/output-metadata.test.ts tests/core/output-sheets.test.ts
git commit -m "feat: add document lookup output contract"
```

---

### Task 2: Add Core Document Lookup Builder

**Files:**
- Create: `src/core/document-lookup.ts`
- Test: `tests/core/document-lookup.test.ts`

- [ ] **Step 1: Write failing core tests**

Create `tests/core/document-lookup.test.ts` with:

```ts
import { describe, expect, it } from "vitest";
import {
  buildDocumentLookupResult,
  buildDocumentLookupSuggestions,
  documentLookupRowsToValues
} from "../../src/core/document-lookup";
import type { RawRow } from "../../src/types/scrap";

function oaRow(overrides: Partial<RawRow> = {}): RawRow {
  return {
    表单编号: "OA-001",
    金蝶云单据编号: "ERP-001",
    申请日期: "2026/5/1",
    公司简称: "数控",
    一级部门: "生产",
    二级部门: "仓储",
    物料代码: "MAT-A",
    物料名称: "物料A",
    数量: 1,
    实际预算金额mx: 10,
    ...overrides
  };
}

function erpRow(overrides: Partial<RawRow> = {}): RawRow {
  return {
    单据编号: "ERP-001",
    日期: "2026/5/2",
    源单单号: "OA-001",
    区分公司简称: "数控",
    一级部门: "生产",
    二级部门: "仓储",
    物料编码: "MAT-A",
    物料名称: "物料A",
    实发数量: 1,
    总成本: 10,
    ...overrides
  };
}

describe("document lookup core", () => {
  it("builds deduplicated OA and ERP suggestions with context labels", () => {
    const suggestions = buildDocumentLookupSuggestions(
      [
        oaRow({ 物料代码: "MAT-A", 金蝶云单据编号: "ERP-001" }),
        oaRow({ 物料代码: "MAT-B", 金蝶云单据编号: "ERP-002", 二级部门: "装配" })
      ],
      [
        erpRow({ 物料编码: "MAT-A", 源单单号: "OA-001" }),
        erpRow({ 物料编码: "MAT-B", 源单单号: "OA-002", 日期: "2026/5/3" })
      ]
    );

    expect(suggestions.oa).toEqual([
      {
        mode: "oa_form_number",
        docNumber: "OA-001",
        label: "OA-001 | 2026-05-01 | 数控 | 生产/仓储,生产/装配 | ERP: ERP-001,ERP-002"
      }
    ]);
    expect(suggestions.erp).toEqual([
      {
        mode: "erp_doc_number",
        docNumber: "ERP-001",
        label: "ERP-001 | 2026-05-02,2026-05-03 | 数控 | 生产/仓储 | OA: OA-001,OA-002"
      }
    ]);
  });

  it("looks up one OA form and pairs materials against linked ERP documents", () => {
    const result = buildDocumentLookupResult({
      mode: "oa_form_number",
      docNumber: "OA-001",
      oaRows: [
        oaRow({ 物料代码: "MAT-A", 数量: 1, 实际预算金额mx: 10 }),
        oaRow({ 物料代码: "MAT-A", 数量: 2, 实际预算金额mx: 20 }),
        oaRow({ 物料代码: "MAT-B", 物料名称: "物料B", 数量: 5, 实际预算金额mx: 50 })
      ],
      erpRows: [
        erpRow({ 物料编码: "MAT-A", 实发数量: 2, 总成本: 25 }),
        erpRow({ 物料编码: "MAT-C", 物料名称: "物料C", 实发数量: 7, 总成本: 70 })
      ]
    });

    expect(result).toEqual({
      ok: true,
      rows: [
        expect.objectContaining({
          rowType: "物料",
          lookupType: "查OA表单编号",
          matchedDocNumber: "OA-001",
          oaItemCode: "MAT-A",
          erpItemCode: "MAT-A",
          oaQuantity: 3,
          erpQuantity: 2,
          quantityDiff: 1,
          amountDiff: 5,
          remark: "数量不同"
        }),
        expect.objectContaining({
          oaItemCode: "MAT-B",
          erpItemCode: "",
          remark: "ERP缺少该物料"
        }),
        expect.objectContaining({
          oaItemCode: "",
          erpItemCode: "MAT-C",
          remark: "OA缺少该物料"
        })
      ]
    });
  });

  it("looks up one ERP document and pairs materials against linked OA forms", () => {
    const result = buildDocumentLookupResult({
      mode: "erp_doc_number",
      docNumber: "ERP-001",
      oaRows: [
        oaRow({ 表单编号: "OA-001", 物料代码: "MAT-A", 数量: 1, 实际预算金额mx: 10 }),
        oaRow({ 表单编号: "OA-002", 物料代码: "MAT-B", 物料名称: "物料B", 数量: 3, 实际预算金额mx: 30 })
      ],
      erpRows: [
        erpRow({ 源单单号: "OA-001", 物料编码: "MAT-A", 实发数量: 1, 总成本: 10 }),
        erpRow({ 源单单号: "OA-002", 物料编码: "MAT-B", 物料名称: "物料B", 实发数量: 4, 总成本: 40 })
      ]
    });

    expect(result.ok).toBe(true);
    if (result.ok) {
      expect(result.rows.map((row) => [row.oaItemCode, row.erpItemCode, row.remark])).toEqual([
        ["MAT-A", "MAT-A", "数量一致"],
        ["MAT-B", "MAT-B", "数量不同"]
      ]);
    }
  });

  it("keeps main OA materials when linked ERP document is missing", () => {
    const result = buildDocumentLookupResult({
      mode: "oa_form_number",
      docNumber: "OA-001",
      oaRows: [oaRow({ 金蝶云单据编号: "ERP-MISSING" })],
      erpRows: []
    });

    expect(result.ok).toBe(true);
    if (result.ok) {
      expect(result.rows).toHaveLength(1);
      expect(result.rows[0]).toMatchObject({
        oaFormNumber: "OA-001",
        erpDocNumber: "",
        remark: "未找到对应ERP单据"
      });
    }
  });

  it("returns a readable no-result message when the selected main document disappears", () => {
    expect(
      buildDocumentLookupResult({
        mode: "oa_form_number",
        docNumber: "OA-MISSING",
        oaRows: [oaRow()],
        erpRows: [erpRow()]
      })
    ).toEqual({ ok: false, message: "未找到OA表单编号：OA-MISSING" });

    expect(
      buildDocumentLookupResult({
        mode: "erp_doc_number",
        docNumber: "ERP-MISSING",
        oaRows: [oaRow()],
        erpRows: [erpRow()]
      })
    ).toEqual({ ok: false, message: "未找到ERP单据编号：ERP-MISSING" });
  });

  it("converts lookup rows to the fixed worksheet header order", () => {
    const result = buildDocumentLookupResult({
      mode: "oa_form_number",
      docNumber: "OA-001",
      oaRows: [oaRow()],
      erpRows: [erpRow()]
    });

    expect(result.ok).toBe(true);
    if (result.ok) {
      expect(documentLookupRowsToValues(result.rows)[0]).toEqual([
        "行类型",
        "查询类型",
        "命中单号",
        "OA表单编号",
        "OA记录的ERP单号",
        "OA申请日期",
        "OA公司简称",
        "OA一级部门",
        "OA二级部门",
        "OA物料编码",
        "OA物料名称",
        "OA数量",
        "OA金额",
        "ERP单据编号",
        "ERP记录的OA单号",
        "ERP日期",
        "ERP公司简称",
        "ERP一级部门",
        "ERP二级部门",
        "ERP物料编码",
        "ERP物料名称",
        "ERP数量",
        "ERP金额",
        "数量差额",
        "金额差额",
        "备注"
      ]);
    }
  });
});
```

- [ ] **Step 2: Run focused core tests and verify they fail**

Run:

```bash
npm test -- tests/core/document-lookup.test.ts
```

Expected: FAIL because `src/core/document-lookup.ts` does not exist.

- [ ] **Step 3: Create the core module exports and implementation**

Create `src/core/document-lookup.ts` with these exported types and functions:

```ts
import type Decimal from "decimal.js-light";
import { DOCUMENT_LOOKUP_HEADERS } from "../constants";
import type { OutputMatrix, RawRow } from "../types/scrap";
import { normalizeDateKey } from "../utils/date";
import { addDecimal, decimalToNumber2, parseDecimal, subtractDecimal, zeroDecimal } from "../utils/decimal";
import { appendUniqueJoinedText, normalizeText } from "../utils/text";

export type DocumentLookupMode = "oa_form_number" | "erp_doc_number";
export type DocumentLookupTypeLabel = "查OA表单编号" | "查ERP单据编号";

export interface DocumentLookupSuggestion {
  mode: DocumentLookupMode;
  docNumber: string;
  label: string;
}

export interface DocumentLookupSuggestions {
  oa: DocumentLookupSuggestion[];
  erp: DocumentLookupSuggestion[];
}

export interface DocumentLookupInput {
  mode: DocumentLookupMode;
  docNumber: string;
  oaRows: RawRow[] | null | undefined;
  erpRows: RawRow[] | null | undefined;
}

export interface DocumentLookupRow {
  rowType: "物料";
  lookupType: DocumentLookupTypeLabel;
  matchedDocNumber: string;
  oaFormNumber: string;
  oaRecordedErpDocNumber: string;
  oaDate: string;
  oaCompany: string;
  oaDept1: string;
  oaDept2: string;
  oaItemCode: string;
  oaItemName: string;
  oaQuantity: number;
  oaAmount: number;
  erpDocNumber: string;
  erpRecordedOaFormNumber: string;
  erpDate: string;
  erpCompany: string;
  erpDept1: string;
  erpDept2: string;
  erpItemCode: string;
  erpItemName: string;
  erpQuantity: number;
  erpAmount: number;
  quantityDiff: number;
  amountDiff: number;
  remark: string;
}

export type DocumentLookupResult =
  | { ok: true; rows: DocumentLookupRow[] }
  | { ok: false; message: string };

interface SuggestionAccumulator {
  docNumber: string;
  dates: string;
  companies: string;
  deptPairs: string;
  counterpartDocNumbers: string;
}

interface MaterialAccumulator {
  formNumbers: string;
  recordedCounterpartDocNumbers: string;
  dates: string;
  companies: string;
  dept1: string;
  dept2: string;
  itemCode: string;
  itemName: string;
  quantity: Decimal;
  amount: Decimal;
}

function appendText(current: string, value: unknown, separator = ","): string {
  return appendUniqueJoinedText(current, normalizeText(value), separator);
}

function deptPair(dept1: unknown, dept2: unknown): string {
  const left = normalizeText(dept1);
  const right = normalizeText(dept2);
  if (left && right) {
    return `${left}/${right}`;
  }
  return left || right;
}

function getOrCreateSuggestion(groups: Map<string, SuggestionAccumulator>, docNumber: string): SuggestionAccumulator {
  const existing = groups.get(docNumber);
  if (existing) {
    return existing;
  }

  const created: SuggestionAccumulator = {
    docNumber,
    dates: "",
    companies: "",
    deptPairs: "",
    counterpartDocNumbers: ""
  };
  groups.set(docNumber, created);
  return created;
}

function buildSuggestionLabel(parts: string[]): string {
  return parts.filter((part) => part.trim()).join(" | ");
}

export function buildDocumentLookupSuggestions(
  oaRows: RawRow[] | null | undefined,
  erpRows: RawRow[] | null | undefined
): DocumentLookupSuggestions {
  const oaGroups = new Map<string, SuggestionAccumulator>();
  for (const row of oaRows ?? []) {
    const docNumber = normalizeText(row["表单编号"]);
    if (!docNumber) {
      continue;
    }
    const target = getOrCreateSuggestion(oaGroups, docNumber);
    target.dates = appendText(target.dates, normalizeDateKey(row["申请日期"]));
    target.companies = appendText(target.companies, row["公司简称"]);
    target.deptPairs = appendText(target.deptPairs, deptPair(row["一级部门"], row["二级部门"]));
    target.counterpartDocNumbers = appendText(target.counterpartDocNumbers, row["金蝶云单据编号"]);
  }

  const erpGroups = new Map<string, SuggestionAccumulator>();
  for (const row of erpRows ?? []) {
    const docNumber = normalizeText(row["单据编号"]);
    if (!docNumber) {
      continue;
    }
    const target = getOrCreateSuggestion(erpGroups, docNumber);
    target.dates = appendText(target.dates, normalizeDateKey(row["日期"]));
    target.companies = appendText(target.companies, row["区分公司简称"]);
    target.deptPairs = appendText(target.deptPairs, deptPair(row["一级部门"], row["二级部门"]));
    target.counterpartDocNumbers = appendText(target.counterpartDocNumbers, row["源单单号"]);
  }

  return {
    oa: [...oaGroups.values()].map((group) => ({
      mode: "oa_form_number",
      docNumber: group.docNumber,
      label: buildSuggestionLabel([
        group.docNumber,
        group.dates,
        group.companies,
        group.deptPairs,
        `ERP: ${group.counterpartDocNumbers}`
      ])
    })),
    erp: [...erpGroups.values()].map((group) => ({
      mode: "erp_doc_number",
      docNumber: group.docNumber,
      label: buildSuggestionLabel([
        group.docNumber,
        group.dates,
        group.companies,
        group.deptPairs,
        `OA: ${group.counterpartDocNumbers}`
      ])
    }))
  };
}
```

Continue the same file with the lookup functions. Keep helpers private except the three exports used by tests and macros:

```ts
function collectRowsByExactText(rows: RawRow[] | null | undefined, fieldName: string, expected: string): RawRow[] {
  const normalizedExpected = normalizeText(expected);
  return (rows ?? []).filter((row) => normalizeText(row[fieldName]) === normalizedExpected);
}

function collectRowsByAnyText(
  rows: RawRow[] | null | undefined,
  fieldName: string,
  expectedValues: Set<string>
): RawRow[] {
  return (rows ?? []).filter((row) => expectedValues.has(normalizeText(row[fieldName])));
}

function collectUniqueText(rows: RawRow[], fieldName: string): Set<string> {
  const result = new Set<string>();
  for (const row of rows) {
    const value = normalizeText(row[fieldName]);
    if (value) {
      result.add(value);
    }
  }
  return result;
}

function getOrCreateMaterial(groups: Map<string, MaterialAccumulator>, itemCode: string): MaterialAccumulator {
  const existing = groups.get(itemCode);
  if (existing) {
    return existing;
  }
  const created: MaterialAccumulator = {
    formNumbers: "",
    recordedCounterpartDocNumbers: "",
    dates: "",
    companies: "",
    dept1: "",
    dept2: "",
    itemCode,
    itemName: "",
    quantity: zeroDecimal(),
    amount: zeroDecimal()
  };
  groups.set(itemCode, created);
  return created;
}

function aggregateOaMaterials(rows: RawRow[]): Map<string, MaterialAccumulator> {
  const result = new Map<string, MaterialAccumulator>();
  for (const row of rows) {
    const itemCode = normalizeText(row["物料代码"]);
    if (!itemCode) {
      continue;
    }
    const target = getOrCreateMaterial(result, itemCode);
    target.formNumbers = appendText(target.formNumbers, row["表单编号"]);
    target.recordedCounterpartDocNumbers = appendText(target.recordedCounterpartDocNumbers, row["金蝶云单据编号"]);
    target.dates = appendText(target.dates, normalizeDateKey(row["申请日期"]));
    target.companies = appendText(target.companies, row["公司简称"]);
    target.dept1 = appendText(target.dept1, row["一级部门"]);
    target.dept2 = appendText(target.dept2, row["二级部门"]);
    if (!target.itemName) {
      target.itemName = normalizeText(row["物料名称"]);
    }
    target.quantity = addDecimal(target.quantity, parseDecimal(row["数量"], "数量"));
    target.amount = addDecimal(target.amount, parseDecimal(row["实际预算金额mx"], "实际预算金额mx"));
  }
  return result;
}

function aggregateErpMaterials(rows: RawRow[]): Map<string, MaterialAccumulator> {
  const result = new Map<string, MaterialAccumulator>();
  for (const row of rows) {
    const itemCode = normalizeText(row["物料编码"]);
    if (!itemCode) {
      continue;
    }
    const target = getOrCreateMaterial(result, itemCode);
    target.formNumbers = appendText(target.formNumbers, row["单据编号"]);
    target.recordedCounterpartDocNumbers = appendText(target.recordedCounterpartDocNumbers, row["源单单号"]);
    target.dates = appendText(target.dates, normalizeDateKey(row["日期"]));
    target.companies = appendText(target.companies, row["区分公司简称"]);
    target.dept1 = appendText(target.dept1, row["一级部门"]);
    target.dept2 = appendText(target.dept2, row["二级部门"]);
    if (!target.itemName) {
      target.itemName = normalizeText(row["物料名称"]);
    }
    target.quantity = addDecimal(target.quantity, parseDecimal(row["实发数量"], "实发数量"));
    target.amount = addDecimal(target.amount, parseDecimal(row["总成本"], "总成本"));
  }
  return result;
}

function unionMaterialCodes(left: Map<string, MaterialAccumulator>, right: Map<string, MaterialAccumulator>): string[] {
  return [...new Set([...left.keys(), ...right.keys()])];
}

function remarkFor(
  oa: MaterialAccumulator | undefined,
  erp: MaterialAccumulator | undefined
): string {
  if (oa && !erp) {
    return "ERP缺少该物料";
  }
  if (!oa && erp) {
    return "OA缺少该物料";
  }
  if (!oa || !erp) {
    return "";
  }
  return subtractDecimal(oa.quantity, erp.quantity).isZero() ? "数量一致" : "数量不同";
}

function buildRows(
  lookupType: DocumentLookupTypeLabel,
  matchedDocNumber: string,
  oaRows: RawRow[],
  erpRows: RawRow[],
  missingCounterpartRemark: string
): DocumentLookupRow[] {
  const oaMaterials = aggregateOaMaterials(oaRows);
  const erpMaterials = aggregateErpMaterials(erpRows);
  const counterpartMissing = lookupType === "查OA表单编号" ? erpRows.length === 0 : oaRows.length === 0;

  return unionMaterialCodes(oaMaterials, erpMaterials).map((itemCode) => {
    const oa = oaMaterials.get(itemCode);
    const erp = erpMaterials.get(itemCode);
    const quantityDiff = subtractDecimal(oa?.quantity ?? zeroDecimal(), erp?.quantity ?? zeroDecimal());
    const amountDiff = subtractDecimal(oa?.amount ?? zeroDecimal(), erp?.amount ?? zeroDecimal());
    const rowMissingRemark = counterpartMissing ? missingCounterpartRemark : remarkFor(oa, erp);

    return {
      rowType: "物料",
      lookupType,
      matchedDocNumber,
      oaFormNumber: oa?.formNumbers ?? "",
      oaRecordedErpDocNumber: oa?.recordedCounterpartDocNumbers ?? "",
      oaDate: oa?.dates ?? "",
      oaCompany: oa?.companies ?? "",
      oaDept1: oa?.dept1 ?? "",
      oaDept2: oa?.dept2 ?? "",
      oaItemCode: oa?.itemCode ?? "",
      oaItemName: oa?.itemName ?? "",
      oaQuantity: decimalToNumber2(oa?.quantity ?? zeroDecimal()),
      oaAmount: decimalToNumber2(oa?.amount ?? zeroDecimal()),
      erpDocNumber: erp?.formNumbers ?? "",
      erpRecordedOaFormNumber: erp?.recordedCounterpartDocNumbers ?? "",
      erpDate: erp?.dates ?? "",
      erpCompany: erp?.companies ?? "",
      erpDept1: erp?.dept1 ?? "",
      erpDept2: erp?.dept2 ?? "",
      erpItemCode: erp?.itemCode ?? "",
      erpItemName: erp?.itemName ?? "",
      erpQuantity: decimalToNumber2(erp?.quantity ?? zeroDecimal()),
      erpAmount: decimalToNumber2(erp?.amount ?? zeroDecimal()),
      quantityDiff: decimalToNumber2(quantityDiff),
      amountDiff: decimalToNumber2(amountDiff),
      remark: rowMissingRemark
    };
  });
}

function buildOaLookup(input: DocumentLookupInput): DocumentLookupResult {
  const oaRows = collectRowsByExactText(input.oaRows, "表单编号", input.docNumber);
  if (oaRows.length === 0) {
    return { ok: false, message: `未找到OA表单编号：${input.docNumber}` };
  }

  const erpDocNumbers = collectUniqueText(oaRows, "金蝶云单据编号");
  const erpRows = collectRowsByAnyText(input.erpRows, "单据编号", erpDocNumbers);
  return { ok: true, rows: buildRows("查OA表单编号", input.docNumber, oaRows, erpRows, "未找到对应ERP单据") };
}

function buildErpLookup(input: DocumentLookupInput): DocumentLookupResult {
  const erpRows = collectRowsByExactText(input.erpRows, "单据编号", input.docNumber);
  if (erpRows.length === 0) {
    return { ok: false, message: `未找到ERP单据编号：${input.docNumber}` };
  }

  const oaFormNumbers = collectUniqueText(erpRows, "源单单号");
  const oaRows = collectRowsByAnyText(input.oaRows, "表单编号", oaFormNumbers);
  return { ok: true, rows: buildRows("查ERP单据编号", input.docNumber, oaRows, erpRows, "未找到对应OA单据") };
}

export function buildDocumentLookupResult(input: DocumentLookupInput): DocumentLookupResult {
  return input.mode === "oa_form_number" ? buildOaLookup(input) : buildErpLookup(input);
}

export function documentLookupRowsToValues(rows: DocumentLookupRow[] | null | undefined): OutputMatrix {
  return [
    [...DOCUMENT_LOOKUP_HEADERS],
    ...(rows ?? []).map((row) => [
      row.rowType,
      row.lookupType,
      row.matchedDocNumber,
      row.oaFormNumber,
      row.oaRecordedErpDocNumber,
      row.oaDate,
      row.oaCompany,
      row.oaDept1,
      row.oaDept2,
      row.oaItemCode,
      row.oaItemName,
      row.oaQuantity,
      row.oaAmount,
      row.erpDocNumber,
      row.erpRecordedOaFormNumber,
      row.erpDate,
      row.erpCompany,
      row.erpDept1,
      row.erpDept2,
      row.erpItemCode,
      row.erpItemName,
      row.erpQuantity,
      row.erpAmount,
      row.quantityDiff,
      row.amountDiff,
      row.remark
    ])
  ];
}
```

- [ ] **Step 4: Run focused core tests and verify they pass**

Run:

```bash
npm test -- tests/core/document-lookup.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit the core builder**

Run:

```bash
git add src/core/document-lookup.ts tests/core/document-lookup.test.ts
git commit -m "feat: add document lookup core builder"
```

---

### Task 3: Add Static Document Lookup Dialog

**Files:**
- Create: `ui/document-lookup-dialog.html`
- Create: `ui/document-lookup-dialog.js`
- Test: `tests/query-dialog/document-lookup-static.test.ts`

- [ ] **Step 1: Write failing static dialog tests**

Create `tests/query-dialog/document-lookup-static.test.ts` with a small VM harness. Use this test content:

```ts
/// <reference types="node" />

import { readFileSync } from "node:fs";
import { resolve } from "node:path";
import vm from "node:vm";
import { describe, expect, it } from "vitest";

const repoRoot = resolve(__dirname, "..", "..");

type Listener = (event: { preventDefault(): void; target?: FakeElement }) => void;

class FakeElement {
  public id = "";
  public name = "";
  public value = "";
  public checked = false;
  public textContent = "";
  public className = "";
  public children: FakeElement[] = [];
  public style: Record<string, string> = {};
  private readonly attributes = new Map<string, string>();
  private readonly listeners = new Map<string, Listener[]>();

  constructor(public readonly tagName: string) {}

  appendChild(child: FakeElement): FakeElement {
    this.children.push(child);
    return child;
  }

  removeChild(child: FakeElement): void {
    this.children = this.children.filter((item) => item !== child);
  }

  addEventListener(type: string, listener: Listener): void {
    const listeners = this.listeners.get(type) ?? [];
    listeners.push(listener);
    this.listeners.set(type, listeners);
  }

  dispatchEvent(type: string): void {
    for (const listener of this.listeners.get(type) ?? []) {
      listener({ preventDefault() {}, target: this });
    }
  }

  setAttribute(name: string, value: string): void {
    this.attributes.set(name, value);
  }

  getAttribute(name: string): string | undefined {
    return this.attributes.get(name);
  }

  getBoundingClientRect(): { left: number; bottom: number; width: number } {
    return { left: 10, bottom: 50, width: 240 };
  }
}

class FakeDocument {
  public readonly body = new FakeElement("body");
  private readonly elements = new Map<string, FakeElement>();
  private readonly modeInputs: FakeElement[] = [];

  constructor() {
    this.register("lookupForm", new FakeElement("form"));
    this.register("documentKeyword", new FakeElement("input"));
    this.register("btnClear", new FakeElement("button"));
    this.register("btnCancel", new FakeElement("button"));
    const oa = new FakeElement("input");
    oa.name = "lookupMode";
    oa.value = "oa_form_number";
    oa.checked = true;
    const erp = new FakeElement("input");
    erp.name = "lookupMode";
    erp.value = "erp_doc_number";
    this.modeInputs.push(oa, erp);
  }

  createElement(tagName: string): FakeElement {
    return new FakeElement(tagName);
  }

  getElementById(id: string): FakeElement | null {
    return this.elements.get(id) ?? null;
  }

  querySelectorAll(selector: string): FakeElement[] {
    return selector === 'input[name="lookupMode"]' ? this.modeInputs : [];
  }

  modeInputsList(): FakeElement[] {
    return this.modeInputs;
  }

  dropdown(): FakeElement {
    const dropdown = this.body.children.find((child) => child.className === "autocomplete-menu");
    if (!dropdown) {
      throw new Error("missing dropdown");
    }
    return dropdown;
  }

  private register(id: string, element: FakeElement): void {
    element.id = id;
    this.elements.set(id, element);
  }
}

interface DialogHooks {
  getMatchedSuggestions(value: string, suggestions: Array<{ docNumber: string; label: string }>): Array<{ docNumber: string; label: string }>;
}

function loadDialog() {
  const document = new FakeDocument();
  const storage = new Map<string, string>();
  const hooks = {} as DialogHooks;
  const alerts: string[] = [];
  storage.set(
    "ScrapVarianceDocumentLookupInitialState:test-token",
    JSON.stringify({
      token: "test-token",
      suggestions: {
        oa: [
          { mode: "oa_form_number", docNumber: "OA-001", label: "OA-001 | 2026-05-01 | 数控 | 生产/仓储 | ERP: ERP-001" }
        ],
        erp: [
          { mode: "erp_doc_number", docNumber: "ERP-001", label: "ERP-001 | 2026-05-02 | 数控 | 生产/仓储 | OA: OA-001" }
        ]
      }
    })
  );
  const windowObject = {
    Application: {
      PluginStorage: {
        getItem(key: string): string | undefined {
          return storage.get(key);
        },
        setItem(key: string, value: string): void {
          storage.set(key, value);
        }
      }
    },
    __SCRAP_VARIANCE_DOCUMENT_LOOKUP_DIALOG_TESTS__: hooks,
    addEventListener() {},
    close() {},
    location: { search: "?token=test-token" },
    pageXOffset: 0,
    pageYOffset: 0,
    setTimeout(callback: () => void): number {
      callback();
      return 1;
    }
  };
  const context = vm.createContext({
    alert(message: string) {
      alerts.push(message);
    },
    document,
    window: windowObject
  });

  vm.runInContext(readFileSync(resolve(repoRoot, "ui/document-lookup-dialog.js"), "utf-8"), context);

  return { document, storage, hooks, alerts };
}

describe("static document lookup dialog", () => {
  it("filters candidates by containment and exposes test hooks", () => {
    const { hooks } = loadDialog();

    expect(
      hooks.getMatchedSuggestions("001", [
        { docNumber: "OA-001", label: "OA-001 | ERP: ERP-001" },
        { docNumber: "OA-ABC", label: "OA-ABC | ERP: ERP-ABC" }
      ])
    ).toEqual([{ docNumber: "OA-001", label: "OA-001 | ERP: ERP-001" }]);
  });

  it("requires selecting a dropdown candidate before submitting", () => {
    const { document, alerts, storage } = loadDialog();
    const input = document.getElementById("documentKeyword");
    input!.value = "OA-001";

    document.getElementById("lookupForm")!.dispatchEvent("submit");

    expect(alerts).toEqual(["请先从下拉候选中选择一个单号。"]);
    expect(storage.get("ScrapVarianceDocumentLookupDialogResult")).toBeUndefined();
  });

  it("submits the selected OA document number, not the display label", () => {
    const { document, storage } = loadDialog();
    const input = document.getElementById("documentKeyword");
    input!.value = "OA";
    input!.dispatchEvent("input");
    document.dropdown().children[0]!.dispatchEvent("mousedown");

    document.getElementById("lookupForm")!.dispatchEvent("submit");

    expect(JSON.parse(String(storage.get("ScrapVarianceDocumentLookupDialogResult")))).toEqual({
      token: "test-token",
      action: "query",
      selection: {
        mode: "oa_form_number",
        docNumber: "OA-001"
      }
    });
  });

  it("keeps lookup mode when clearing the text input", () => {
    const { document } = loadDialog();
    const [, erp] = document.modeInputsList();
    erp!.checked = true;

    document.getElementById("btnClear")!.dispatchEvent("click");

    expect(erp!.checked).toBe(true);
    expect(document.getElementById("documentKeyword")!.value).toBe("");
  });
});
```

- [ ] **Step 2: Run static dialog tests and verify they fail**

Run:

```bash
npm test -- tests/query-dialog/document-lookup-static.test.ts
```

Expected: FAIL because `ui/document-lookup-dialog.js` does not exist.

- [ ] **Step 3: Create the HTML shell**

Create `ui/document-lookup-dialog.html`:

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8">
    <title>单号查询</title>
    <style>
      body {
        box-sizing: border-box;
        margin: 0;
        padding: 18px;
        font-family: "Microsoft YaHei", Arial, sans-serif;
        color: #1f2933;
        background: #f7f8fa;
      }

      .grid {
        display: grid;
        grid-template-columns: 96px 1fr;
        gap: 12px;
        align-items: center;
      }

      .field-label {
        font-size: 14px;
        text-align: right;
      }

      .mode-group {
        display: flex;
        gap: 14px;
        margin: 0;
        padding: 0;
        border: 0;
      }

      .mode-option {
        display: inline-flex;
        align-items: center;
        gap: 5px;
        font-size: 14px;
        white-space: nowrap;
      }

      .text-input {
        box-sizing: border-box;
        width: 100%;
        height: 30px;
        border: 1px solid #c9d1d9;
        border-radius: 4px;
        padding: 4px 8px;
        font-size: 14px;
        background: #fff;
      }

      .autocomplete-menu {
        position: absolute;
        z-index: 1000;
        box-sizing: border-box;
        display: none;
        max-height: 240px;
        overflow-y: auto;
        border: 1px solid #c9d1d9;
        border-radius: 4px;
        background: #fff;
        box-shadow: 0 4px 12px rgba(31, 41, 51, 0.14);
      }

      .autocomplete-option {
        box-sizing: border-box;
        min-height: 28px;
        padding: 5px 8px;
        font-size: 13px;
        line-height: 18px;
        cursor: pointer;
      }

      .autocomplete-option:hover {
        background: #e6f4ef;
      }

      .actions {
        display: flex;
        justify-content: flex-end;
        gap: 8px;
        margin-top: 18px;
      }

      button {
        min-width: 72px;
        height: 30px;
        border: 1px solid #9aa4b2;
        border-radius: 4px;
        background: #fff;
        cursor: pointer;
      }

      button.primary {
        border-color: #087f5b;
        background: #099268;
        color: #fff;
      }
    </style>
  </head>
  <body>
    <form id="lookupForm">
      <div class="grid">
        <div class="field-label">查询类型</div>
        <fieldset class="mode-group">
          <label class="mode-option">
            <input type="radio" name="lookupMode" value="oa_form_number" checked>
            <span>查 OA 表单编号</span>
          </label>
          <label class="mode-option">
            <input type="radio" name="lookupMode" value="erp_doc_number">
            <span>查 ERP 单据编号</span>
          </label>
        </fieldset>
        <label class="field-label" for="documentKeyword">单号</label>
        <input id="documentKeyword" class="text-input" name="documentKeyword" autocomplete="off">
      </div>
      <div class="actions">
        <button class="primary" type="submit">查询</button>
        <button id="btnClear" type="button">清空</button>
        <button id="btnCancel" type="button">取消</button>
      </div>
    </form>
    <script src="./document-lookup-dialog.js"></script>
  </body>
</html>
```

- [ ] **Step 4: Create the static dialog script**

Create `ui/document-lookup-dialog.js` with ES5-style functions. Keep the result key and initial-state key names exactly as shown:

```js
(function () {
  var RESULT_KEY = "ScrapVarianceDocumentLookupDialogResult";
  var INITIAL_KEY_PREFIX = "ScrapVarianceDocumentLookupInitialState:";
  var MAX_VISIBLE_OPTIONS = 30;
  var selectedSuggestion = null;
  var hasSubmitted = false;
  var dropdown = null;

  function decodeQueryPart(value) {
    try {
      return decodeURIComponent(String(value).replace(/\+/g, " "));
    } catch (error) {
      return "";
    }
  }

  function getQueryParam(name) {
    var search = window.location && window.location.search ? String(window.location.search) : "";
    var query = search.charAt(0) === "?" ? search.slice(1) : search;
    var parts;
    var index;
    var pair;
    var key;
    if (!query) {
      return "";
    }
    parts = query.split("&");
    for (index = 0; index < parts.length; index += 1) {
      pair = parts[index].split("=");
      key = decodeQueryPart(pair[0]);
      if (key === name) {
        return decodeQueryPart(pair.slice(1).join("="));
      }
    }
    return "";
  }

  function getToken() {
    return getQueryParam("token");
  }

  function getStorage(showAlert) {
    var app = window.Application;
    if (!app || !app.PluginStorage) {
      if (showAlert !== false) {
        alert("当前 WPS 环境不支持 PluginStorage，无法提交单号查询。");
      }
      return null;
    }
    return app.PluginStorage;
  }

  function normalizeSuggestionList(input) {
    var result = [];
    var index;
    var item;
    if (!input || typeof input.length !== "number") {
      return result;
    }
    for (index = 0; index < input.length; index += 1) {
      item = input[index];
      if (item && typeof item.docNumber === "string" && typeof item.label === "string") {
        result.push(item);
      }
    }
    return result;
  }

  function readInitialPayload() {
    var storage = getStorage(false);
    var token = getToken();
    var raw;
    var parsed;
    if (!storage || !token) {
      return null;
    }
    raw = storage.getItem(INITIAL_KEY_PREFIX + token);
    if (typeof raw !== "string" || !raw.trim()) {
      return null;
    }
    try {
      parsed = JSON.parse(raw);
    } catch (error) {
      return null;
    }
    return parsed && parsed.token === token ? parsed : null;
  }

  function readSuggestions() {
    var payload = readInitialPayload();
    var suggestions = payload && payload.suggestions ? payload.suggestions : {};
    return {
      oa: normalizeSuggestionList(suggestions.oa),
      erp: normalizeSuggestionList(suggestions.erp)
    };
  }

  function getLookupMode() {
    var inputs = document.querySelectorAll('input[name="lookupMode"]');
    var index;
    for (index = 0; index < inputs.length; index += 1) {
      if (inputs[index].checked) {
        return inputs[index].value === "erp_doc_number" ? "erp_doc_number" : "oa_form_number";
      }
    }
    return "oa_form_number";
  }

  function currentSuggestions() {
    var suggestions = readSuggestions();
    return getLookupMode() === "erp_doc_number" ? suggestions.erp : suggestions.oa;
  }

  function getMatchedSuggestions(value, suggestions) {
    var query = String(value || "").trim();
    var result = [];
    var index;
    for (index = 0; index < suggestions.length; index += 1) {
      if (!query || suggestions[index].label.indexOf(query) !== -1 || suggestions[index].docNumber.indexOf(query) !== -1) {
        result.push(suggestions[index]);
      }
      if (result.length >= MAX_VISIBLE_OPTIONS) {
        break;
      }
    }
    return result;
  }

  function ensureDropdown() {
    if (dropdown) {
      return dropdown;
    }
    dropdown = document.createElement("div");
    dropdown.className = "autocomplete-menu";
    document.body.appendChild(dropdown);
    return dropdown;
  }

  function hideDropdown() {
    if (!dropdown) {
      return;
    }
    dropdown.style.display = "none";
    while (dropdown.children && dropdown.children.length) {
      dropdown.removeChild(dropdown.children[0]);
    }
  }

  function positionDropdown(input, menu) {
    var rect = input.getBoundingClientRect();
    menu.style.left = rect.left + (window.pageXOffset || 0) + "px";
    menu.style.top = rect.bottom + (window.pageYOffset || 0) + "px";
    menu.style.width = rect.width + "px";
  }

  function renderSuggestions() {
    var input = document.getElementById("documentKeyword");
    var menu = ensureDropdown();
    var matches = getMatchedSuggestions(input.value, currentSuggestions());
    var index;
    var option;
    hideDropdown();
    for (index = 0; index < matches.length; index += 1) {
      option = document.createElement("div");
      option.className = "autocomplete-option";
      option.textContent = matches[index].label;
      option.addEventListener("mousedown", (function (suggestion) {
        return function (event) {
          event.preventDefault();
          selectedSuggestion = {
            mode: getLookupMode(),
            docNumber: suggestion.docNumber
          };
          input.value = suggestion.label;
          hideDropdown();
        };
      })(matches[index]));
      menu.appendChild(option);
    }
    if (matches.length) {
      positionDropdown(input, menu);
      menu.style.display = "block";
    }
  }

  function clearSelectionOnly() {
    selectedSuggestion = null;
    document.getElementById("documentKeyword").value = "";
    hideDropdown();
  }

  function submitResult(action, selection) {
    var storage = getStorage();
    var token = getToken();
    if (!storage || !token) {
      alert("单号查询弹窗缺少会话标识，请关闭后重新打开。");
      return;
    }
    storage.setItem(
      RESULT_KEY,
      JSON.stringify({
        token: token,
        action: action,
        selection: selection || null
      })
    );
    hasSubmitted = true;
    if (typeof window.close === "function") {
      window.close();
    }
  }

  function writeCancelIfNeeded() {
    if (hasSubmitted) {
      return;
    }
    submitResult("cancel", null);
  }

  function initialize() {
    var input = document.getElementById("documentKeyword");
    var modeInputs = document.querySelectorAll('input[name="lookupMode"]');
    var index;

    input.addEventListener("input", function () {
      selectedSuggestion = null;
      renderSuggestions();
    });
    input.addEventListener("focus", renderSuggestions);
    input.addEventListener("blur", function () {
      window.setTimeout(hideDropdown, 120);
    });

    for (index = 0; index < modeInputs.length; index += 1) {
      modeInputs[index].addEventListener("change", clearSelectionOnly);
    }

    document.getElementById("lookupForm").addEventListener("submit", function (event) {
      event.preventDefault();
      if (!selectedSuggestion) {
        alert("请先从下拉候选中选择一个单号。");
        return;
      }
      submitResult("query", selectedSuggestion);
    });
    document.getElementById("btnClear").addEventListener("click", clearSelectionOnly);
    document.getElementById("btnCancel").addEventListener("click", function () {
      submitResult("cancel", null);
    });
    window.addEventListener("beforeunload", writeCancelIfNeeded);

    if (window.__SCRAP_VARIANCE_DOCUMENT_LOOKUP_DIALOG_TESTS__) {
      window.__SCRAP_VARIANCE_DOCUMENT_LOOKUP_DIALOG_TESTS__.getMatchedSuggestions = getMatchedSuggestions;
    }
  }

  initialize();
})();
```

- [ ] **Step 5: Run static dialog tests and verify they pass**

Run:

```bash
npm test -- tests/query-dialog/document-lookup-static.test.ts
```

Expected: PASS.

- [ ] **Step 6: Commit the static dialog**

Run:

```bash
git add ui/document-lookup-dialog.html ui/document-lookup-dialog.js tests/query-dialog/document-lookup-static.test.ts
git commit -m "feat: add document lookup dialog UI"
```

---

### Task 4: Add Document Lookup Dialog Bridge

**Files:**
- Create: `src/query-dialog/open-document-lookup-dialog.ts`
- Test: `tests/query-dialog/open-document-lookup-dialog.test.ts`

- [ ] **Step 1: Write failing bridge tests**

Create `tests/query-dialog/open-document-lookup-dialog.test.ts`:

```ts
import { afterEach, describe, expect, it, vi } from "vitest";
import {
  DOCUMENT_LOOKUP_DIALOG_RESULT_KEY,
  openDocumentLookupDialogAndRun,
  pollDocumentLookupDialogResult
} from "../../src/query-dialog/open-document-lookup-dialog";
import type { DocumentLookupSelection, DocumentLookupSuggestions } from "../../src/core/document-lookup";
import type { ScrapVarianceGlobal } from "../../src/types/wps";

type ShowDialogMock = ReturnType<
  typeof vi.fn<(url: string, title: string, width: number, height: number, modal: boolean) => unknown>
>;

function makeRoot(): ScrapVarianceGlobal & {
  Application: NonNullable<ScrapVarianceGlobal["Application"]> & {
    PluginStorage: {
      values: Map<string, unknown>;
      getItem(key: string): unknown;
      setItem(key: string, value: unknown): void;
      removeItem(key: string): void;
    };
    ShowDialog: ShowDialogMock;
  };
} {
  const values = new Map<string, unknown>();
  return {
    Application: {
      PluginStorage: {
        values,
        getItem(key: string): unknown {
          return values.get(key);
        },
        setItem(key: string, value: unknown): void {
          values.set(key, value);
        },
        removeItem(key: string): void {
          values.delete(key);
        }
      },
      ShowDialog: vi.fn()
    }
  };
}

function suggestions(): DocumentLookupSuggestions {
  return {
    oa: [{ mode: "oa_form_number", docNumber: "OA-001", label: "OA-001 | ERP: ERP-001" }],
    erp: [{ mode: "erp_doc_number", docNumber: "ERP-001", label: "ERP-001 | OA: OA-001" }]
  };
}

function tokenFromDialog(root: ReturnType<typeof makeRoot>): string {
  const [url] = root.Application.ShowDialog.mock.calls[0] ?? [];
  return new URL(String(url)).searchParams.get("token") ?? "";
}

function initialKey(token: string): string {
  return `ScrapVarianceDocumentLookupInitialState:${token}`;
}

afterEach(() => {
  vi.useRealTimers();
});

describe("document lookup dialog bridge", () => {
  it("opens the document lookup dialog with tokenized suggestions", () => {
    vi.useFakeTimers();
    const root = makeRoot();
    const runLookup = vi.fn();
    const reportError = vi.fn();

    openDocumentLookupDialogAndRun(root, suggestions(), runLookup, reportError);

    const [url, title, width, height, modal] = root.Application.ShowDialog.mock.calls[0] ?? [];
    const token = tokenFromDialog(root);
    expect(String(url)).toContain("ui/document-lookup-dialog.html");
    expect(title).toBe("单号查询");
    expect(width).toBeGreaterThanOrEqual(480);
    expect(height).toBeGreaterThanOrEqual(240);
    expect(modal).toBe(false);
    expect(JSON.parse(String(root.Application.PluginStorage.values.get(initialKey(token))))).toEqual({
      token,
      suggestions: suggestions()
    });
    vi.clearAllTimers();
  });

  it("runs a submitted matching-token lookup and clears storage", () => {
    const root = makeRoot();
    const runLookup = vi.fn();
    const reportError = vi.fn();
    root.Application.PluginStorage.values.set(
      DOCUMENT_LOOKUP_DIALOG_RESULT_KEY,
      JSON.stringify({
        token: "token-1",
        action: "query",
        selection: { mode: "oa_form_number", docNumber: "OA-001" }
      })
    );
    root.Application.PluginStorage.values.set(initialKey("token-1"), JSON.stringify({ token: "token-1", suggestions: suggestions() }));

    expect(pollDocumentLookupDialogResult(root, "token-1", runLookup, reportError)).toBe(true);

    expect(runLookup).toHaveBeenCalledWith({ mode: "oa_form_number", docNumber: "OA-001" } satisfies DocumentLookupSelection);
    expect(root.Application.PluginStorage.values.has(initialKey("token-1"))).toBe(false);
    expect(root.Application.PluginStorage.values.get(DOCUMENT_LOOKUP_DIALOG_RESULT_KEY)).toBe("");
  });

  it("ignores stale tokens and cancels without running lookup", () => {
    const root = makeRoot();
    const runLookup = vi.fn();
    const reportError = vi.fn();
    root.Application.PluginStorage.values.set(
      DOCUMENT_LOOKUP_DIALOG_RESULT_KEY,
      JSON.stringify({ token: "other-token", action: "query", selection: { mode: "oa_form_number", docNumber: "OA-001" } })
    );

    expect(pollDocumentLookupDialogResult(root, "token-1", runLookup, reportError)).toBe(false);
    expect(runLookup).not.toHaveBeenCalled();

    root.Application.PluginStorage.values.set(DOCUMENT_LOOKUP_DIALOG_RESULT_KEY, JSON.stringify({ token: "token-1", action: "cancel" }));
    expect(pollDocumentLookupDialogResult(root, "token-1", runLookup, reportError)).toBe(true);
    expect(runLookup).not.toHaveBeenCalled();
  });

  it("reports invalid submitted selection without running lookup", () => {
    const root = makeRoot();
    const runLookup = vi.fn();
    const reportError = vi.fn();
    root.Application.PluginStorage.values.set(
      DOCUMENT_LOOKUP_DIALOG_RESULT_KEY,
      JSON.stringify({ token: "token-1", action: "query", selection: { mode: "bad", docNumber: "" } })
    );

    expect(pollDocumentLookupDialogResult(root, "token-1", runLookup, reportError)).toBe(true);

    expect(runLookup).not.toHaveBeenCalled();
    expect(reportError).toHaveBeenCalledWith(new Error("单号查询结果无效：请重新选择单号。"));
  });

  it("clears tokenized initial state when ShowDialog throws synchronously", () => {
    vi.useFakeTimers();
    const root = makeRoot();
    root.Application.ShowDialog.mockImplementation(() => {
      throw new Error("ShowDialog failed");
    });

    expect(() => openDocumentLookupDialogAndRun(root, suggestions(), vi.fn(), vi.fn())).toThrow("ShowDialog failed");

    const token = tokenFromDialog(root);
    expect(root.Application.PluginStorage.values.has(initialKey(token))).toBe(false);
    vi.clearAllTimers();
  });
});
```

- [ ] **Step 2: Run bridge tests and verify they fail**

Run:

```bash
npm test -- tests/query-dialog/open-document-lookup-dialog.test.ts
```

Expected: FAIL because `src/query-dialog/open-document-lookup-dialog.ts` does not exist.

- [ ] **Step 3: Create the bridge module**

Create `src/query-dialog/open-document-lookup-dialog.ts`:

```ts
import type {
  DocumentLookupSelection,
  DocumentLookupSuggestions
} from "../core/document-lookup";
import type { ScrapVarianceGlobal } from "../types/wps";

export const DOCUMENT_LOOKUP_DIALOG_RESULT_KEY = "ScrapVarianceDocumentLookupDialogResult";

const INITIAL_STATE_KEY_PREFIX = "ScrapVarianceDocumentLookupInitialState:";
const DIALOG_TIMEOUT_MS = 5 * 60 * 1000;
const DIALOG_POLL_MS = 250;

type DialogAction = "query" | "cancel";
type RunLookup = (selection: DocumentLookupSelection) => void;
type ReportError = (error: unknown) => void;

interface DocumentLookupDialogResult {
  token: string;
  action: DialogAction;
  selection?: Partial<DocumentLookupSelection> | null;
}

function getStorage(root: ScrapVarianceGlobal) {
  const storage = root.Application?.PluginStorage;
  if (!storage) {
    throw new Error("当前 WPS 环境不支持 PluginStorage，无法打开单号查询弹窗。");
  }
  return storage;
}

function clearDialogResult(root: ScrapVarianceGlobal): void {
  getStorage(root).setItem(DOCUMENT_LOOKUP_DIALOG_RESULT_KEY, "");
}

function initialStateKey(token: string): string {
  return `${INITIAL_STATE_KEY_PREFIX}${token}`;
}

function clearInitialState(root: ScrapVarianceGlobal, token: string): void {
  const storage = getStorage(root);
  const key = initialStateKey(token);
  if (typeof storage.removeItem === "function") {
    storage.removeItem(key);
    return;
  }
  storage.setItem(key, "");
}

function createDialogToken(): string {
  return `${Date.now()}-${Math.random().toString(36).slice(2)}`;
}

function buildDialogUrl(token: string): string {
  const base =
    typeof globalThis.location?.href === "string" ? globalThis.location.href : "http://127.0.0.1:3889/index.html";
  const url = new URL("ui/document-lookup-dialog.html", base);
  url.searchParams.set("token", token);
  return url.toString();
}

function writeInitialState(root: ScrapVarianceGlobal, token: string, suggestions: DocumentLookupSuggestions): void {
  getStorage(root).setItem(initialStateKey(token), JSON.stringify({ token, suggestions }));
}

function readDialogResult(root: ScrapVarianceGlobal): DocumentLookupDialogResult | null {
  const raw = getStorage(root).getItem(DOCUMENT_LOOKUP_DIALOG_RESULT_KEY);
  if (typeof raw !== "string" || !raw.trim()) {
    return null;
  }

  try {
    const parsed = JSON.parse(raw) as Partial<DocumentLookupDialogResult>;
    if (typeof parsed.token !== "string" || (parsed.action !== "query" && parsed.action !== "cancel")) {
      return null;
    }
    return {
      token: parsed.token,
      action: parsed.action,
      selection: parsed.selection
    };
  } catch {
    return null;
  }
}

function normalizeSelection(selection: Partial<DocumentLookupSelection> | null | undefined): DocumentLookupSelection {
  const mode = selection?.mode;
  const docNumber = typeof selection?.docNumber === "string" ? selection.docNumber.trim() : "";
  if ((mode !== "oa_form_number" && mode !== "erp_doc_number") || !docNumber) {
    throw new Error("单号查询结果无效：请重新选择单号。");
  }
  return { mode, docNumber };
}

function buildTimeoutError(): Error {
  return new Error("单号查询弹窗超时：没有收到查询或取消结果，请关闭弹窗后重试。");
}

export function pollDocumentLookupDialogResult(
  root: ScrapVarianceGlobal,
  token: string,
  runLookup: RunLookup,
  reportError: ReportError
): boolean {
  const result = readDialogResult(root);
  if (!result || result.token !== token) {
    return false;
  }

  clearDialogResult(root);
  clearInitialState(root, token);
  if (result.action === "cancel") {
    return true;
  }

  try {
    runLookup(normalizeSelection(result.selection));
  } catch (error) {
    reportError(error);
  }
  return true;
}

export function openDocumentLookupDialogAndRun(
  root: ScrapVarianceGlobal,
  suggestions: DocumentLookupSuggestions,
  runLookup: RunLookup,
  reportError: ReportError
): void {
  const application = root.Application;
  if (typeof application?.ShowDialog !== "function") {
    throw new Error("当前 WPS 环境不支持 ShowDialog，无法打开单号查询弹窗。");
  }

  const token = createDialogToken();
  clearDialogResult(root);
  writeInitialState(root, token, suggestions);
  try {
    application.ShowDialog(buildDialogUrl(token), "单号查询", 560, 300, false);
  } catch (error) {
    clearDialogResult(root);
    clearInitialState(root, token);
    throw error;
  }

  const startedAt = Date.now();
  const timer = globalThis.setInterval(() => {
    if (pollDocumentLookupDialogResult(root, token, runLookup, reportError)) {
      globalThis.clearInterval(timer);
      return;
    }
    if (Date.now() - startedAt > DIALOG_TIMEOUT_MS) {
      globalThis.clearInterval(timer);
      clearDialogResult(root);
      clearInitialState(root, token);
      reportError(buildTimeoutError());
    }
  }, DIALOG_POLL_MS);
}
```

- [ ] **Step 4: Run bridge tests and verify they pass**

Run:

```bash
npm test -- tests/query-dialog/open-document-lookup-dialog.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit the bridge**

Run:

```bash
git add src/query-dialog/open-document-lookup-dialog.ts tests/query-dialog/open-document-lookup-dialog.test.ts
git commit -m "feat: add document lookup dialog bridge"
```

---

### Task 5: Add Document Lookup Macro And Worksheet Writing

**Files:**
- Create: `src/macros/document-lookup.ts`
- Test: `tests/macros/document-lookup.test.ts`

- [ ] **Step 1: Write failing macro tests**

Create `tests/macros/document-lookup.test.ts`:

```ts
import { describe, expect, it, vi } from "vitest";
import { ERP_REQUIRED_HEADERS, OA_REQUIRED_HEADERS, SHEET_NAMES } from "../../src/constants";
import { runDocumentLookupWithSelection, startDocumentLookup } from "../../src/macros/document-lookup";
import type { ScrapVarianceGlobal } from "../../src/types/wps";
import { createFakeApplication, createFakeSheet, type FakeSheet } from "../wps-api/fakes";

function makeRoot(sheets: FakeSheet[]): ScrapVarianceGlobal {
  return {
    Application: createFakeApplication(sheets)
  };
}

function oaSource(): FakeSheet {
  return createFakeSheet(SHEET_NAMES.oa, [
    [...OA_REQUIRED_HEADERS],
    ["OA-001", "ERP-001", "2026/5/1", "数控", "生产", "仓储", "MAT-A", "物料A", 1, 10]
  ]);
}

function erpSource(): FakeSheet {
  return createFakeSheet(SHEET_NAMES.erp, [
    [...ERP_REQUIRED_HEADERS],
    ["ERP-001", "2026/5/2", "OA-001", "数控", "生产", "仓储", "MAT-A", "物料A", 1, 10]
  ]);
}

function sheetNames(root: ScrapVarianceGlobal): string[] {
  const sheets = root.Application?.ActiveWorkbook?.Worksheets;
  if (!sheets) {
    throw new Error("missing fake worksheets");
  }
  const result: string[] = [];
  for (let index = 1; index <= sheets.Count; index += 1) {
    result.push(sheets.Item(index).Name);
  }
  return result;
}

describe("document lookup macro", () => {
  it("creates the fixed result sheet and writes document lookup output", () => {
    const root = makeRoot([oaSource(), erpSource()]);

    runDocumentLookupWithSelection(root, { mode: "oa_form_number", docNumber: "OA-001" });

    expect(sheetNames(root)).toContain(SHEET_NAMES.documentLookup);
    const resultSheet = root.Application?.ActiveWorkbook?.Worksheets?.Item(3) as FakeSheet;
    expect(resultSheet.Name).toBe(SHEET_NAMES.documentLookup);
    expect(resultSheet.writes[0]?.address).toBe("A1:Z2");
    expect(resultSheet.writes[0]?.value).toEqual([
      expect.arrayContaining(["行类型", "查询类型", "命中单号", "OA表单编号", "ERP单据编号", "备注"]),
      expect.arrayContaining(["物料", "查OA表单编号", "OA-001", "OA-001", "ERP-001", "数量一致"])
    ]);
    expect(resultSheet.rangeValues.get("CB1:CC1")).toEqual([["document_lookup", "A1:Z2"]]);
  });

  it("reuses the fixed result sheet and clears previous document lookup output", () => {
    const resultSheet = createFakeSheet(SHEET_NAMES.documentLookup);
    resultSheet.rangeValues.set("CB1:CC1", [["document_lookup", "A1:Z9"]]);
    const root = makeRoot([oaSource(), erpSource(), resultSheet]);

    runDocumentLookupWithSelection(root, { mode: "erp_doc_number", docNumber: "ERP-001" });

    expect(sheetNames(root).filter((name) => name === SHEET_NAMES.documentLookup)).toHaveLength(1);
    expect(resultSheet.clears).toEqual(["A1:Z9"]);
    expect(resultSheet.writes[0]?.address).toBe("A1:Z2");
  });

  it("writes a readable no-result row when the selected document is no longer present", () => {
    const root = makeRoot([oaSource(), erpSource()]);

    runDocumentLookupWithSelection(root, { mode: "oa_form_number", docNumber: "OA-MISSING" });

    const resultSheet = root.Application?.ActiveWorkbook?.Worksheets?.Item(3) as FakeSheet;
    expect(resultSheet.writes[0]?.value).toEqual([["提示", "未找到OA表单编号：OA-MISSING"]]);
    expect(resultSheet.rangeValues.get("CB1:CC1")).toEqual([["document_lookup", "A1:B1"]]);
  });

  it("does not open the dialog when source sheets are missing", () => {
    const root = makeRoot([]);

    expect(() => startDocumentLookup(root, vi.fn(), vi.fn())).toThrow("找不到工作表：查询OA-存货报废申请单");
  });
});
```

- [ ] **Step 2: Run macro tests and verify they fail**

Run:

```bash
npm test -- tests/macros/document-lookup.test.ts
```

Expected: FAIL because `src/macros/document-lookup.ts` does not exist.

- [ ] **Step 3: Create the macro module**

Create `src/macros/document-lookup.ts`:

```ts
import {
  ERP_REQUIRED_HEADERS,
  MAX_HEADER_SCAN_ROWS,
  MIN_ERP_HEADER_MATCH_COUNT,
  MIN_OA_HEADER_MATCH_COUNT,
  OA_REQUIRED_HEADERS,
  SHEET_NAMES,
  WRITE_CHUNK_ROWS
} from "../constants";
import {
  buildDocumentLookupResult,
  buildDocumentLookupSuggestions,
  documentLookupRowsToValues,
  type DocumentLookupSelection
} from "../core/document-lookup";
import { openDocumentLookupDialogAndRun } from "../query-dialog/open-document-lookup-dialog";
import type { OutputMatrix, RawRow } from "../types/scrap";
import type { ScrapVarianceGlobal, WpsSheet } from "../types/wps";
import { clearPreviousToolOutput, saveOutputMetadata } from "../wps-api/output-metadata";
import { readSheetTable } from "../wps-api/read-sheet-data";
import { getSheetByName, ensureSheet } from "../wps-api/workbook";
import { rangeAddress, writeMatrixBulkOrChunks } from "../wps-api/write-results";

interface SourceRows {
  oaRows: RawRow[];
  erpRows: RawRow[];
}

type ReportError = (error: unknown) => void;

function matrixWidth(values: OutputMatrix): number {
  return values.reduce((width, row) => Math.max(width, row.length), 0);
}

function readSourceRows(root?: ScrapVarianceGlobal): SourceRows {
  const oaSheet = getSheetByName(SHEET_NAMES.oa, root);
  const erpSheet = getSheetByName(SHEET_NAMES.erp, root);
  const oaTable = readSheetTable(oaSheet, [...OA_REQUIRED_HEADERS], MIN_OA_HEADER_MATCH_COUNT, MAX_HEADER_SCAN_ROWS);
  const erpTable = readSheetTable(
    erpSheet,
    [...ERP_REQUIRED_HEADERS],
    MIN_ERP_HEADER_MATCH_COUNT,
    MAX_HEADER_SCAN_ROWS
  );
  return {
    oaRows: oaTable.rows,
    erpRows: erpTable.rows
  };
}

function writeValuesWithMetadata(sheet: WpsSheet, values: OutputMatrix): void {
  const width = matrixWidth(values);
  if (values.length === 0 || width === 0) {
    return;
  }

  const address = rangeAddress(1, 1, values.length, width);
  writeMatrixBulkOrChunks(sheet, 1, 1, values, WRITE_CHUNK_ROWS);
  saveOutputMetadata(sheet, { kind: "document_lookup", rangeAddress: address });
}

export function runDocumentLookupWithSelection(
  root: ScrapVarianceGlobal | undefined,
  selection: DocumentLookupSelection
): void {
  const { oaRows, erpRows } = readSourceRows(root);
  const result = buildDocumentLookupResult({
    mode: selection.mode,
    docNumber: selection.docNumber,
    oaRows,
    erpRows
  });
  const sheet = ensureSheet(SHEET_NAMES.documentLookup, root);

  clearPreviousToolOutput(sheet, "document_lookup");
  writeValuesWithMetadata(sheet, result.ok ? documentLookupRowsToValues(result.rows) : [["提示", result.message]]);
}

export function startDocumentLookup(
  root: ScrapVarianceGlobal,
  reportError: ReportError,
  runLookup: (selection: DocumentLookupSelection) => void = (selection) => runDocumentLookupWithSelection(root, selection)
): void {
  const { oaRows, erpRows } = readSourceRows(root);
  openDocumentLookupDialogAndRun(root, buildDocumentLookupSuggestions(oaRows, erpRows), runLookup, reportError);
}
```

- [ ] **Step 4: Run macro tests and verify they pass**

Run:

```bash
npm test -- tests/macros/document-lookup.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit the macro**

Run:

```bash
git add src/macros/document-lookup.ts tests/macros/document-lookup.test.ts
git commit -m "feat: write document lookup results"
```

---

### Task 6: Wire The Ribbon Button And Build Tests

**Files:**
- Modify: `src/actions/button-actions.ts`
- Modify: `src/main.ts`
- Modify: `ribbon.xml`
- Modify: `tests/actions/button-actions.test.ts`
- Modify: `tests/build/build-output.test.ts`
- Modify after build: `main.js`

- [ ] **Step 1: Write failing action/build tests**

Modify the runner object in `tests/actions/button-actions.test.ts`:

```ts
    const runners = {
      runPrecheck: vi.fn(),
      setupOutputSheets: vi.fn(),
      queryCurrentSheet: vi.fn(),
      lookupDocument: vi.fn(),
      toggleMaterialRows: vi.fn(),
      runDiagnostics: vi.fn()
    };
```

Update the expected action keys:

```ts
    expect(Object.keys(actions)).toEqual([
      "btnPrecheck",
      "btnSetupOutputSheets",
      "btnQueryCurrentSheet",
      "btnLookupDocument",
      "btnToggleMaterialRows",
      "btnPerformanceDiagnostics"
    ]);
```

Add the name assertion:

```ts
    expect(actions.btnLookupDocument.name).toBe("lookupDocument");
```

Modify `tests/build/build-output.test.ts`.

In `"keeps ribbon.xml pointed at the bundled ribbon object"`, add:

```ts
    expect(xml).toContain('id="btnLookupDocument"');
```

In `"ships the static query dialog page"`, rename the test to `"ships the static query dialog pages"` and add:

```ts
    const lookupHtml = readText("ui/document-lookup-dialog.html");
    const lookupScript = readText("ui/document-lookup-dialog.js");

    expect(lookupHtml).toContain('id="lookupForm"');
    expect(lookupHtml).toContain('name="lookupMode"');
    expect(lookupHtml).toContain('id="documentKeyword"');
    expect(lookupHtml).toContain('src="./document-lookup-dialog.js"');
    expect(lookupScript).toContain("ScrapVarianceDocumentLookupDialogResult");
    expect(lookupScript).toContain("ScrapVarianceDocumentLookupInitialState:");
    expect(lookupScript).toContain("__SCRAP_VARIANCE_DOCUMENT_LOOKUP_DIALOG_TESTS__");
    expect(lookupScript).not.toContain("URLSearchParams");
```

In `"bundles the real ribbon handlers instead of the temporary placeholder"`, add:

```ts
    expect(searchableSource).toContain("btnLookupDocument");
```

- [ ] **Step 2: Run focused tests and verify they fail**

Run:

```bash
npm test -- tests/actions/button-actions.test.ts tests/build/build-output.test.ts
```

Expected: FAIL because the button is not registered and the bundle is stale.

- [ ] **Step 3: Add the action runner and registry entry**

Modify `src/actions/button-actions.ts`.

Add to `ButtonActionRunners`:

```ts
  lookupDocument(): unknown | Promise<unknown>;
```

Add to the object returned by `createButtonActions()` after `btnQueryCurrentSheet`:

```ts
    btnLookupDocument: {
      name: "lookupDocument",
      run: runners.lookupDocument
    },
```

- [ ] **Step 4: Bind the macro in `src/main.ts`**

Modify imports:

```ts
import { startDocumentLookup } from "./macros/document-lookup";
```

Add to `createDefaultButtonActions()` after `queryCurrentSheet`:

```ts
    lookupDocument: () => startDocumentLookup(root, reportRuntimeError),
```

- [ ] **Step 5: Add the ribbon button**

Modify `ribbon.xml` after `btnQueryCurrentSheet`:

```xml
          <button id="btnLookupDocument" label="查单号" size="large" onAction="ribbon.OnAction" />
```

- [ ] **Step 6: Run focused tests before building and confirm only bundle-sync checks fail**

Run:

```bash
npm test -- tests/actions/button-actions.test.ts tests/build/build-output.test.ts
```

Expected: action tests PASS; build output test may FAIL at `keeps committed main.js in sync with the esbuild output` because `main.js` has not been regenerated yet.

- [ ] **Step 7: Regenerate `main.js`**

Run:

```bash
npm run build
```

Expected: PASS and `main.js` changes.

- [ ] **Step 8: Run focused tests and verify they pass**

Run:

```bash
npm test -- tests/actions/button-actions.test.ts tests/build/build-output.test.ts
```

Expected: PASS.

- [ ] **Step 9: Commit the button wiring and bundle**

Run:

```bash
git add src/actions/button-actions.ts src/main.ts ribbon.xml tests/actions/button-actions.test.ts tests/build/build-output.test.ts main.js
git commit -m "feat: wire document lookup button"
```

---

### Task 7: Full Verification

**Files:**
- No planned source edits unless verification exposes a concrete failure.

- [ ] **Step 1: Run TypeScript build**

Run:

```bash
npm run build
```

Expected: PASS.

- [ ] **Step 2: Run full test suite**

Run:

```bash
npm test
```

Expected: PASS.

- [ ] **Step 3: Check whitespace**

Run:

```bash
git diff --check
```

Expected: no output.

- [ ] **Step 4: Confirm committed bundle is synchronized**

Run:

```bash
git status --short
```

Expected: no unstaged `main.js` changes after the latest `npm run build`.

- [ ] **Step 5: Commit any verification-only fixes**

If Step 1 through Step 4 required a small concrete fix, commit it:

```bash
git add <changed-files>
git commit -m "fix: stabilize document lookup verification"
```

If Step 1 through Step 4 pass without source changes, do not create an empty commit.
